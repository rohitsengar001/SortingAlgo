void InsertionSort(int a[],int range)
{
  int i,j,key;
  for (i = 1; i < range; i += 1)
  {
    key=a[i];
    j=i-1;
    while(j>=0 && a[j]>key)
    {
       a[j+1]=a[j];
       j--;
    }
    a[j+1]=key;
  }//for loop end
}
